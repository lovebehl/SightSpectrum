import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  login: {
    padding: 8,
  },
  forgot: {
    marginTop: 12,
  },
  labelStyle: {
    fontSize: 12,
  },
  TextInputStyles: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
  },
});

export default styles;
